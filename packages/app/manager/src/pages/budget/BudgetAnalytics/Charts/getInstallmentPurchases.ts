export default function getInstallmentPurchases() {
    // const total = releases
    //     .filter(d => d.type === 'exit')
    //     .reduce((acc, current) => acc += current.value, 0);

    // const installmentPurchases = releases
    //     .filter(i => i.installments.total > 1)
    //     .reduce((acc, current) => {

    //     }, {});
};